<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StatueApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:StatueApp.ViewModel"
    x:Class="StatueApp.View.BlankPage1"
    mc:Ignorable="d">
    <Page.DataContext>
        <ViewModel:ViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" RequestedTheme="Dark">
        <ComboBox ItemsSource="{Binding StatueSingleton.StatueTypes, UpdateSourceTrigger=PropertyChanged}" Margin="537,301,0,0"/>
        <ComboBox x:Name="comboBox" HorizontalAlignment="Left" Margin="214,174,0,0" VerticalAlignment="Top" Width="120" ItemsSource="{Binding PlacementSingleton.Placements, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <!--<ListView x:Name="listView" HorizontalAlignment="Left" Height="100" Margin="81,171,0,0" VerticalAlignment="Top" Width="482" ItemsSource="{Binding StatueSingleton.StatueTypes, UpdateSourceTrigger=PropertyChanged}"/>-->
    </Grid>
</Page>
